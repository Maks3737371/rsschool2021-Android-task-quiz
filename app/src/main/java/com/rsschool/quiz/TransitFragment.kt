package com.rsschool.quiz

interface TransitFragment {
    fun TransitQuestion(questionNum: Int)
    fun TransitResult(result: String, message: String)
}

/*
package com.rsschool.quiz

interface TransitFragment {
    fun TransitQuestion(numberQuestion: Int)
    fun TransitResult(result: String, message: String)
}*/
